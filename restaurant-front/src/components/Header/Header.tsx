import { HeaderContainer, HeaderTitle, HeaderWrapper } from "./Header.styles";

const Header = () => {
    return (
        <HeaderWrapper>
            <HeaderContainer>
                <   HeaderTitle>Home Page</HeaderTitle>
            </HeaderContainer>
        </HeaderWrapper>
    );
}

export default Header